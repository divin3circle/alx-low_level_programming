#include "main.h"

/**
 * _isupper - checks whether an alphabet passed is of uppercase
 * @c: integer to be passed
 * Return: 1 if c is upper or 0 otherwise
 */

int _isupper(int c)
{
	if (c = 'A' && c = 'Z')
		return (1);
	else
		return (0);
}
