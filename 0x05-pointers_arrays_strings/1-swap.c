#include "main.h"

/**
* swap_int - swaps the values of int passed
* @a: adress of int one
* @b: adress of second integer
* Return: void always
*/
void swap_int(int *a, int *b)
{
	int c = *a;
	*a = *b;
	*m = c;
}
